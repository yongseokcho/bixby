layout-macro-def(RecipeOneImage) {
  params {
    param (recipeBasicStructure) {
      type (RecipeBasicStructure)
      min (Required)
      max (One)
    }
  }
  
  content {
    section {
      content {
        // image {
        //   aspect-ratio (21:9)
        //   url ("[#{value(recipe.image.url)}]")
        // }
        image-carousel {
          images (recipeBasicStructure.images)
        }
      }
    }
  }
}
