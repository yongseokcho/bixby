action (GetIngredientsByName) {
  description ("검색어를 포함하는 재료 검색")
  type (Search)
  
  collect {
    input (name) {
      type (IngredientSymbol)
      min (Required) max (Many)
    }
  }
  
  output (Ingredient)
}
