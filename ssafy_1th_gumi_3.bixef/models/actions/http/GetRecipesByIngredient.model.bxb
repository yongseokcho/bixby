action (GetRecipesByIngredient) {
  description ("재료별 레시피 조회")
  type (Search)
  
  collect {
    input (name) {
      type (IngredientSymbol)
      min (Required) max (Many)
    }
  }
  
  output (Ingredient)
}
